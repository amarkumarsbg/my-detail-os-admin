# SaaS Admin Portal
# Browser API base (no trailing slash). Leave empty to use Next.js rewrite + BACKEND_URL.
NEXT_PUBLIC_API_URL=http://localhost:4000

# Server-only rewrite target when NEXT_PUBLIC_API_URL is empty (see next.config.js)
# BACKEND_URL=http://localhost:4000
